name: Manual Workflow

on:
  workflow_dispatch:   # 👈 This makes the workflow manually triggerable from GitHub Actions UI

jobs:
  run-manual-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run Script
        run: echo "This workflow runs only when triggered manually!"